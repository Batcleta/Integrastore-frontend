
const getDomIdElement = (element) => document.getElementById(element)
const getDomClass = (element) => document.getElementsByClassName(element)
const queryAll = (element) => document.querySelectorAll(element)
